# 📊 김치프리미엄 델타 뉴트럴 차익거래 전략 명세 (Ver 3.0)

## 1. 프로젝트 개요

### 1.1 목표
- **전략**: Upbit(현물)와 Binance(선물) 간의 김치프리미엄 괴리를 이용한 무위험(Delta Neutral) 차익거래
- **핵심 철학**: **"절대 손절하지 않는다 (No Stop Loss)"**
- **문제 해결**: 김프가 하락 후 반등하지 않고 횡보하는 'L자형 장세'에서 자금이 묶이는 문제를 **'삼각 수렴 돌파(Volatility Breakout)'** 로직으로 해결

### 1.2 운용 구조
```
┌─────────────────────────────────────────────────────────────┐
│                     Delta Neutral Position                    │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│   ┌─────────────┐              ┌─────────────┐              │
│   │   Upbit     │              │  Binance    │              │
│   │  현물 LONG   │     +       │  선물 SHORT  │   = 0 Delta  │
│   │   50%       │              │    50%      │              │
│   └─────────────┘              └─────────────┘              │
│                                                               │
│   김프 상승 → Upbit 이익 + Binance 손실 = 순수익             │
│   김프 하락 → Upbit 손실 + Binance 이익 = 대기               │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 전략 발전 과정 (History)

### Phase 1: 초기 전략 (Z-Score Reversion)
- **로직**: Z-Score -2.0 이탈 후 회귀 시 진입, +0.5% 수익 시 청산
- **결과**: ❌ 실패 - 진입 후 목표 수익률(0.7%)에 도달하지 못하고 장기 보유(물림) 상태 발생
- **원인**: 환율이 급등하는 구간에서 김프는 구조적으로 하락/횡보, 반등 탄력 부족

### Phase 2: 수정 제안 및 기각
- **제안**: 환율 필터 + 타임컷(24시간 후 강제 청산)
- **결과**: ❌ 기각 - 횡보장에서 잦은 손절(Time-cut)로 인해 수수료 손실 누적
- **결정**: "손절 로직 폐기" → No Stop Loss 철학 확정

### Phase 3: 최종 전략 확정 (Breakout Rescue) ✅
- **개선안**:
  1. **환율 급등 필터**: 환율이 추세보다 높으면 아예 진입 금지 (방어)
  2. **무손절 존버**: 진입 후 물리면 무기한 보유
  3. **구조대(Rescue) 로직**: '볼린저 밴드 상단 돌파' 시점에 약수익(수수료+0.1%)으로 탈출

### 3개월 스트레스 테스트 결과
| 항목 | 결과 |
|:---|:---|
| **테스트 기간** | 3개월 (버블→폭락→횡보→회복 시뮬레이션) |
| **승률** | 100% (68전 68승) |
| **누적 수익률** | +11.09% |
| **특이사항** | 거래의 56%가 'Breakout Rescue'로 탈출 |

---

## 3. 최종 전략 명세 (PRD)

### 3.1 핵심 파라미터

| 항목 | 설정값 | 설명 |
|:---|:---|:---|
| **운용 자본** | 4,000만원 | 현물 50% / 선물 50% (레버리지 1x) |
| **예비비** | 5% (200만원) | 수수료/슬리피지 대응 |
| **실제 운용** | 95% (3,800만원) | Level 1 + Level 2 합계 |
| **수수료 (왕복)** | **0.38%** | Upbit(0.05×2) + Binance(0.04×2) + 슬리피지(0.1) |
| **정상 목표 (Track A)** | **0.7%** | Gross Profit 기준 (순수익 약 0.32%) |
| **탈출 목표 (Track B)** | **0.48%** | Breakout 시 최소 수익 (순수익 0.1% 확보) |
| **지표 기준** | 1분 봉 | 매매 판단은 1초 단위 실행 |

### 3.2 데이터 소스

| 데이터 | 소스 | 갱신 주기 |
|:---|:---|:---|
| **환율 (USD/KRW)** | TradingView `FX_IDC:USDKRW` | 10초~1분 (캐싱) |
| **Upbit 가격** | Upbit WebSocket | 실시간 |
| **Binance 가격** | Binance WebSocket | 실시간 |

---

## 4. 진입 로직 (Entry)

### 4.1 Step 1: 환율 필터 (안전장치) 🛡️

```
조건: 현재_환율 > 환율_12시간_MA × 1.001
결과: 진입 금지
```

- 환율이 12시간 이동평균 대비 0.1% 이상 급등한 경우 진입 차단
- 환율 급등 구간에서는 김프가 구조적으로 하락하는 경향이 있음

### 4.2 Step 2: Z-Score 진입

```
┌─────────────────────────────────────────────────────────────┐
│                      Z-Score Entry Logic                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│   Level 1 (40% 진입)                                         │
│   ├─ 조건: 최근 5분 최저 Z ≤ -2.0 AND 현재 Z > -2.0 (회귀)   │
│   └─ 자본: 1,520만원 (3,800 × 40%)                          │
│                                                               │
│   Level 2 (60% 추가 진입)                                    │
│   ├─ 조건: 최근 5분 최저 Z ≤ -2.5 AND 현재 Z > -2.5 (회귀)   │
│   └─ 자본: 2,280만원 (3,800 × 60%)                          │
│                                                               │
│   급락 케이스 (100% 한번에)                                   │
│   ├─ 조건: Level 1 스킵하고 -2.5 직행 후 회귀                 │
│   └─ 자본: 3,800만원 (전액)                                  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 회귀 판단 로직

```python
def check_reversion(zscore_history: list, current_zscore: float) -> str:
    """
    최근 5분(300초) 내 최저 Z-Score와 현재 Z-Score 비교
    """
    recent_min = min(zscore_history[-300:])  # 5분간 최저점
    
    # 급락 케이스: -2.0 건너뛰고 -2.5 직행 후 회귀
    if recent_min <= -2.5 and current_zscore > -2.5:
        if not has_level1_position():
            return "ENTER_FULL"  # 100% 진입
        else:
            return "ENTER_LEVEL2"  # 60% 추가
    
    # 일반 케이스: -2.0 회귀
    elif recent_min <= -2.0 and current_zscore > -2.0:
        return "ENTER_LEVEL1"  # 40% 진입
    
    return "WAIT"
```

---

## 5. 청산 로직 (Exit) - Dual Track

### 5.1 개요

```
┌─────────────────────────────────────────────────────────────┐
│                    Dual Track Exit System                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│   Track A: 정상 익절 (Target Hit)                            │
│   ├─ 조건: (현재김프 - 평단김프) ≥ 0.7%                      │
│   ├─ 행동: 전량 청산                                         │
│   └─ 기록: exit_reason = 'Target'                           │
│                                                               │
│   Track B: 돌파 탈출 (Breakout Rescue) 🚀                    │
│   ├─ 조건 1: (현재김프 - 평단김프) ≥ 0.48% (최소 마진)       │
│   ├─ 조건 2: 현재김프 > BB_Upper(20, 2.0)                   │
│   ├─ 행동: 전량 청산                                         │
│   └─ 기록: exit_reason = 'Breakout'                         │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 Track A: 정상 익절 (Target Hit)

```python
def check_target_exit(entry_kimp: float, current_kimp: float) -> bool:
    """
    목표 수익률 도달 시 청산
    """
    profit_pct = current_kimp - entry_kimp
    return profit_pct >= 0.7  # 0.7% 이상
```

### 5.3 Track B: 돌파 탈출 (Breakout Rescue)

```python
def check_breakout_exit(
    entry_kimp: float, 
    current_kimp: float,
    bb_upper: float
) -> bool:
    """
    볼린저 밴드 상단 돌파 + 최소 마진 확보 시 탈출
    """
    profit_pct = current_kimp - entry_kimp
    min_margin = 0.48  # 수수료(0.38%) + 순익(0.1%)
    
    # 두 조건 모두 충족해야 탈출
    margin_ok = profit_pct >= min_margin
    breakout_ok = current_kimp > bb_upper
    
    return margin_ok and breakout_ok
```

### 5.4 볼린저 밴드 설정

| 파라미터 | 값 | 설명 |
|:---|:---|:---|
| **대상 데이터** | 김프 % 시계열 | 가격이 아닌 프리미엄 퍼센트 |
| **기간 (Period)** | 20 | 20개 1분봉 |
| **표준편차 배수** | 2.0 | Upper = MA20 + 2×σ |

```python
def calculate_bollinger_band(kimp_series: list, period: int = 20, std_mult: float = 2.0):
    """
    김프 % 시계열 기반 볼린저 밴드 계산
    """
    ma = sum(kimp_series[-period:]) / period
    variance = sum((x - ma) ** 2 for x in kimp_series[-period:]) / period
    std = variance ** 0.5
    
    return {
        'upper': ma + std_mult * std,
        'middle': ma,
        'lower': ma - std_mult * std
    }
```

---

## 6. 실행 타이밍

| 구분 | 주기 | 설명 |
|:---|:---|:---|
| **데이터 수집** | 1초 | WebSocket 실시간 수신 |
| **환율 갱신** | 10초~1분 | TradingView API 호출 후 캐싱 |
| **지표 계산** | 1분 | 1분봉 Resampling 후 계산 |
| **신호 판단** | 1초 | 진입/청산 조건 체크 |
| **주문 실행** | 즉시 | 시장가 주문 |

---

## 7. 상태 관리

### 7.1 포지션 상태

```sql
-- exit_reason 값
'Target'    -- Track A: 정상 목표가 도달
'Breakout'  -- Track B: BB 돌파 탈출
```

### 7.2 상태 전이도

```
┌─────────┐    환율 OK + Z-Score 회귀    ┌─────────┐
│  IDLE   │ ─────────────────────────→ │  OPEN   │
└─────────┘                             └─────────┘
     ▲                                       │
     │                                       │
     │   ┌─────────────────────────────────┐ │
     │   │  Track A: 수익률 ≥ 0.7%         │ │
     │   │  Track B: 수익률 ≥ 0.48%        │ │
     └───│           + BB 상단 돌파         │◀┘
         └─────────────────────────────────┘
```

---

## 8. 향후 과제

1. **실전 봇 개발**: 소액 테스트 진행
2. **자금 관리**: 최대 2주 이상 포지션 보유 가능성 대비
3. **Fakeout 방지**: 3틱 평균 스무딩 로직 추가 고려
4. **환율 데이터 안정성**: TradingView 백업 소스 확보

---

**버전**: 3.0  
**작성일**: 2025-12-14  
**레포**: trading-platform-research
